# Copy to .env (gitignored) and fill with your Entra app registration values.
# These are public-client IDs (not secrets), but keep them out of git for this project.

YAADEIN_ENTRA_CLIENT_ID=
YAADEIN_ENTRA_TENANT_ID=
